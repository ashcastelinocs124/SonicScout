# Short-term Memory

## 2026-05-02 — Smart team-page discovery
- **What:** Replaced `discoverFounders`'s blind `extract({ urls: [companyUrl] })` with a two-stage `pickTeamUrls`. Path A: cheerio anchor scrape over homepage → `gpt-5-mini` picks ≤3 URLs from the real nav. Path B fallback: Firecrawl `fc.map()` → same picker over URL paths. Picker constrained to URLs in candidate list (no hallucinated 404s). Wired into `discoverFounders` — when picker returns `[]`, we skip Firecrawl `/extract` entirely (saves credits). New module `src/ingest/teamPicker.ts`, 8 new tests in `tests/ingest/teamPicker.test.ts`, 2 new tests in `founders.test.ts`. 60 tests total, typecheck clean. Commits: `28e5461`, `e8493be`, `6491e35`, `bb77e19`, `66af648` (refactor: rename + constants + drop unused mocks), `6ef433e`. Plan: `docs/plans/2026-05-02-smart-team-discovery-implementation.md`. Design: `docs/plans/2026-05-02-smart-team-discovery-design.md`.
- **Outcome:** Founder section now gets real team-page URLs to extract from instead of homepage noise. Built via subagent-driven workflow with 2-stage review (spec then quality) per wave. Quality reviewer caught 3 importants on first pass (misleading `*ForTests` helper name, magic numbers `50/3/500`, unused mocks) — all fixed in `66af648` before Task 5 wiring. Final reviewer verdict: READY TO MERGE.
- **Why:** Live demo on Harvey.ai (run #4) produced an all-`[speculative]` founder section because `founderProfiles=[]`. Investigation revealed `discoverFounders` was hitting `urls: [companyUrl]` (just the homepage) — the original 2026-04-30 design's path fan-out to `/team`, `/about`, `/leadership` had never been implemented. LLM-driven URL picking is more general than a hardcoded path list (handles non-standard `/our-people`, `/who-we-are`, etc.).

## 2026-04-25 — DealSense brainstorm + design + implementation
- **What:** Brainstormed DealSense (AI venture analyst for Decasonic) via superpowers:brainstorming. Locked architecture: Slack-first, 7-agent pipeline (Ingestion → 5 parallel specialists → Memo Synthesis), static `thesis.md`, confidence tiering on every claim, recommendation = Pass/Watch/Take Meeting/Invest.
- **Outcome:** Design doc + implementation plan written to `docs/plans/`. All 15 plan tasks implemented sequentially via direct implementation (subagent-driven was rejected as overkill for greenfield). 31/31 tests pass, typecheck clean. No remote push yet.
- **Why:** This is the v1 — pitch-ready demo for Decasonic. Real Slack workspace + Anthropic API key needed to run live.

## 2026-04-28 — LLM provider swap: Anthropic → OpenAI
- **What:** Hard-swapped the LLM layer from `@anthropic-ai/sdk` to `openai`. Models: gpt-5-mini for the 5 specialists + follow-up, gpt-5 for memo synthesis. Removed the `cacheSystem` flag from `LLMArgs` (OpenAI auto-caches prompts >1024 tokens — explicit cache_control is Anthropic-only). Touched: `src/agents/llm.ts`, `specialist.ts`, `memo.ts`, `followup.ts`, `tests/agents/llm.test.ts`, `tests/agents/specialists.test.ts` (replaced cacheSystem assertion with model assertion), `package.json` (`openai@^6.35.0`), `.env.example` (`OPENAI_API_KEY`), `README.md`, `CLAUDE.md`.
- **Outcome:** Typecheck clean, all 31/31 tests pass. Zod-v4 peer-dep conflict required openai v6 (not v4 — captured in learnings.md).
- **Why:** User switched to an OpenAI key. Also rotated/flagged a Slack bot token that leaked in chat — must reinstall Slack app before going live.

## 2026-04-29 — Slack → web surface swap (20-task plan)
- **What:** Brainstormed + planned + implemented localhost web app to replace Slack adapter for live VC-pitch demos. Stack: Express + SSE backend, Vite + React + Tailwind v4 + shadcn frontend, wouter routing, in-process EventEmitter bus connecting BullMQ worker to browser. Schema migrated to drop slack_* columns. Three visual states on `/runs/:id` (loading / progress / completed). 5 agent cards light up as agents complete in parallel. Inline confidence pills.
- **Outcome:** 39/39 backend tests pass, typecheck clean, prod build serves single-binary on `:3000`. Plan: `docs/plans/2026-04-29-dealsense-web-implementation.md` (20 tasks, 4 waves). Started subagent-driven; pivoted to direct execution after T6 (subagent overhead exceeded task time for small tasks).
- **Why:** Slack required workspace setup before each demo, recording was awkward, polish ceiling capped by Block Kit. Web app gives unlimited polish + permalink demos.
